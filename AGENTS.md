# AGENTS.md — OpenCode Rules

## 语言规则

- **必须始终使用中文回复用户**，包括解释、提问、确认、总结等一切沟通
- 代码中的注释、变量名可以用英文，但所有面向用户的对话必须是中文
- Git commit message 使用中文

## 项目规则

本项目的完整上下文、技术栈、架构说明、已完成功能列表等详见 `CLAUDE.md`。
该文件通过 `opencode.json` 的 `instructions` 字段自动加载，无需重复维护。
